@model List<HoiTroWebsite.Models.RoomType>

<div class="our_room">
    <div class="container">
        @foreach (var i in Model)
        {
            <div class="row">
                <div class="col-md-12">
                    <div class="titlepage">
                        <h2>@i.title</h2>
                        <p>Lorem Ipsum available, but the majority have suffered </p>
                    </div>
                </div>
            </div>
            <div class="row">
                @Html.Action("getInfor", "HomePage", new { id = @i.id })
            </div>
        }

    </div>
</div>
<!-- end our_room -->